export type productTypes = {
    id: string,
    category: string,
    name: string,
    image: string,
    carousel?: string[],
    bottom: boolean,
    top_position?: boolean
    description?: string,
    dimensions?: string
}